# PR #18 Agent B 独立复审与定点修复 — 2026-09-09

本记录只覆盖本地备份与还原。对远端原 head `8798b1381f033533ed2b50b144468b7049789c4e` 的结论为 **PATCH REQUIRED**。本记录所在独立分支提供补丁，须由 Agent A 接回、Agent A/C 独立复审；Agent B 不批准或合并自己的修复。分支名为 `codex/pr18-backup-review-fixes`，不并发修改 `feature/backup-restore`。

## 基线与证据身份

重新读取根进度文档、ADR、模块说明和 CI，并查询 GitHub REST/GraphQL 的提交、reviews、inline comments、issue comments、threads 及其分页。17 条 PR #18 thread 均为 unresolved；该标记未被用作缺陷仍存在的依据。未发现适用 AGENTS.md。原工作区的未跟踪文件保持原样。

PR #10/#16 的全部评论来源单独登记在 [任务 0 后续复核队列](task0-pr10-pr16-review-queue-2026-09-09.md)。此轮只完成它们的基线/原始材料读取和处置入队，不把未进行的产品复核标为审查通过。

| PR | 实际 head | API base SHA | 状态 / CI run | 实际 CI checkout |
| --- | --- | --- | --- | --- |
| [18](https://github.com/fwzm/MediaHub/pull/18) | `8798b1381f033533ed2b50b144468b7049789c4e` | `8e516e40568e7d2eb309a1853f14a9c6c4ddc0b1` | OPEN / 34265001710 success | 合成 merge `ac1d53bb3d81f5a55606094f68f182be2d2c1a1d` |
| [10](https://github.com/fwzm/MediaHub/pull/10) | `3febdf8b77a6a1d63d54b9d0c409de708ed978bf` | `0e08fadbb398d0d35ab960f0e73ad8ee6ae2bba7` | OPEN / 33971603657 success | build、API 32、API 36 均为 literal head `3febdf8…` |
| [16](https://github.com/fwzm/MediaHub/pull/16) | `f40363ce5199e05a4cd197b456a851c2b984492b` | `0e08fadbb398d0d35ab960f0e73ad8ee6ae2bba7` | OPEN / 33967890069 success | 合成 merge `b3800d2bb1dab2297700a56c0542e1944798dbbf` |

当前 fetch 的 `origin/main` 为 `8e516e40568e7d2eb309a1853f14a9c6c4ddc0b1`。PR #10/#16 的 API base 与它不同，两者并列报告。PR #18 check-run head 对应 PR head，但实际 checkout 是上表的 merge；该旧 CI 不验证本轮独立补丁。PR #18 原有 CI 命令漏掉 standalone JVM test，22 个 Android unit task 中 12 executed、8 FROM-CACHE、2 NO-SOURCE，不能笼统称为所有测试实际重跑。

本地原 head 在独立 detached worktree 以 `--rerun-tasks --no-build-cache` 实际执行备份原有测试：Crypto 8 + Serializer 22 + Repository 19 + ViewModel 10 + DataSource 4 + FileStore 6 = **69，0 失败**；core:common 另有 ServerAddressNormalizer 27 项，不计入上述备份专项。旧正文的 63 与 69 不再作为当前补丁数字。

## 评论处置表

下列位置均为仓库相对路径。缩写：Serializer/Guard 位于 `core/common/src/main/kotlin/com/mediahub/core/common/backup/`；Repository/DataSource/Journal/Snapshot/FileStore/VM/Screen/Invalidator 位于 `feature/settings/src/main/kotlin/com/mediahub/feature/settings/backup/`，文件名分别为 BackupSerializer.kt、BackupUrlGuard.kt、BackupRepository.kt、BackupDataSource.kt、RestoreJournal.kt、RestoreSnapshotStore.kt、BackupFileStore.kt、BackupViewModel.kt、BackupScreen.kt、TokenSessionLoginInvalidator.kt。每行列出 baseline 判断与本轮处理，不依赖 resolved/outdated 标记。测试名为可搜索标识，执行结果在后文统一列示。

| 来源 / 评论 ID | 原问题 | 代码位置与 8798b13 基线判断 | 处理理由与回归约束 |
| --- | --- | --- | --- |
| Copilot 3944583750 | 缺 Room 事务 | DataSource `applyRestorePlan`；原问题已修 | 保留 `db.withTransaction`；新增真实 SQLite trigger 在首个写入后 abort，反向断言服务器/线路/进度完全不变 |
| Copilot 3944583758 | ExportReady 不启动 SAF | Screen；原问题已修 | 保留正式 launcher；补消费事件、重建不重复启动、失败可重试；VM 与真实 SAF 链路验证 |
| Copilot 3944583763 | appVersion 硬编码 | Screen `backupAppVersion`；仍存在 | 使用 PackageManager 真版本；测试修改包版本元数据，仪器测试解密磁盘产物核对 |
| Copilot 3944583768 | 替换确认永真 | Screen/Repository；原确认已修 | 保留两层确认；策略改变重建预览并清确认；无确认断言零 apply |
| Copilot 3944583774 | 空/坏读取仍索要密码 | VM/FileStore；原 typed 分支已修 | 保留 Empty/TooLarge/ReadFailed；补 close 异常与协程兜底；读取失败不进入密码态 |
| Copilot 3944583782 | 有界读取注释不实 | Serializer/FileStore；原无界读取已修 | 明确 ByteArray 前的 SAF 边界；新增最多消费 max+1 字节且关闭流的生产测试 |
| Codex 3944594974 | 缺 SAF 启动 | Screen；同 3944583758 | 保留已修入口，不重写功能；消费事件与真实 DocumentsUI 验证 |
| Codex 3944594976 | IO/KDF 主线程 | Repository/FileStore；原已修 | 保留 `withContext(dispatchers.io)`；静态验证真实边界，不拿 JVM 测试调度器证明 UI 帧率 |
| Codex 3944594978 | 缺替换确认 | Screen/Repository；同 3944583768 | 保留显式确认，增加真实策略预览约束 |
| Codex 3944594980 | readBytes 后才检查 | FileStore；原已使用分块读取 | 加严每次请求剩余额度；生产计数流验证早停和关闭 |
| Codex 3944594981 | salt 异常逃出 | Serializer；原已有捕获 | 保留类型化错误；明确 32 字节 salt、12 字节 nonce 与上下界，不回显不可信算法字符串 |
| Codex 3944594983 | Room 无事务 | DataSource；同 3944583750 | 真实 SQLite 故障回滚与事务内基线复核 |
| Codex 3944594986 | MERGE 双默认 | `materializeRestorePlan`；仍存在 | 本机默认优先，统一最终唯一性；本机及新导入均默认的反向测试先失败 |
| Codex 3944594989 | 换源残留凭据 | Repository/Invalidator；仍存在 | 身份变化或新 ID 清理 Token/Vault/Emby+Jellyfin session/Account；同源保留；身份失效阶段失败/取消时 Room/偏好零写入；恢复续作与逆向回滚清理 |
| Codex 3944594993 | 旧进度覆盖新进度 | Repository；原 newer-wins 已修 | 保留；改用 Pair 键避免含斜杠 ID 碰撞；DataSource 也保护 newer/equal，冲突来源进度不写入 |
| Codex 3944594996 | minimumReaderVersion 忽略 | Serializer；原已修 | 保留 readerVersion 拒绝；补 section/count/内外版本一致性，非法枚举在深度验证拒绝 |
| Codex 3944594997 | 自导出超过导入上限 | Serializer；原字节上限已修 | 保留 10 MiB；补导出统一结构验证及 LOCAL 空线路自导入；记录数量上限 |
| review 5125954378 suppressed-1 | null outputStream 假成功 | FileStore；原已修 | 保留显式 null 失败；生产文件与错误路径、零成功状态断言 |
| review 5125954378 suppressed-2 | 超大文件伪装空文件 | FileStore；原已修 | 保留 TooLarge 独立分支，补真实消费上限 |
| review 5125954378 suppressed-3 | 嵌套 launch 丢取消归属 | VM；原 operation ownership 已修 | 保留单操作/epoch；补启动前取消、拒绝重入密码擦除、重复 callback 不覆盖完成态 |
| review 5125965193 | Codex 总评 | 无额外独立问题 | 对应上列 11 条 inline，不重复计缺陷 |
| review 5144494796 tests | 既有测试夸大 replace/preview/同 ID 覆盖 | RepositoryTest 与新增 Safety/生产测试 | 读取原断言并保留有效用例；新增精确 before/after、无写入、同源保留、异源失效和真实故障反向断言 |
| review 5144494796 B1 | 导出/流错误链 | Screen/FileStore/VM | 已修部分保留；新增 close 异常与错误脱敏，SAF 取消/保存/自解密仪器链 |
| review 5144494796 B2 | 确认/事务/保护/恢复/基线 | Repository/DataSource/Journal/Snapshot | 冻结计划、事务内 CAS、完整加密 image、明确阶段与回滚；不宣称跨存储 ACID |
| review 5144494796 B3 | 同 ID 身份/凭据/URL | Repository/Invalidator/Guard | type + 规范化地址 + 原始账号；逐线路检查解码 query；身份代际与持久凭据清理 |
| review 5144494796 B4 | 读取边界/错误/线程/导出上限 | FileStore/Serializer/Repository | 保留已存在 IO dispatcher，补 max+1、close/cancel、双向校验 |
| review 5144494796 B5 | envelope/payload/重复/引用/范围 | Serializer/Repository | 参数白名单、完整 counts、Pair 键、类型化拒绝；孤立引用预览披露不落库 |
| review 5144494796 B6 | 操作生命周期/迟到结果 | VM | epoch 与事件消费；密码擦除 finally/completion，错误密码有正式重试入口 |
| review 5145721257 R1 | 失效失败仍写、恢复跳过失效 | Repository/Invalidator；仍存在 | 失败中止、CE 原义传播；forward/rollback 均处理身份，真实 durable clear 与迟到登录响应负向回归 |
| review 5145721257 R2 | commit(false)/损坏误判无日志 | Journal；仍存在 | 所有实例共享互斥、同步 commit 检查、损坏/缺失区分、失败后缓存标为不可信、禁止覆盖待处理日志 |
| review 5145721257 R3 | 回滚漏新行/偏好/完整字段 | Repository/DataSource/Snapshot；仍存在 | 完整前后 image 保留字段，新增行反向删除，nullable 偏好恢复，凭据不复活；文件重开与 OS 进程终止验证分开 |
| review 5145721257 R4 | 预览 MERGE/执行重算/基线无校验 | Repository/VM/Screen；仍存在 | 绑定 validated 实例、strategy、冻结 plan；预览后及事务内变化拒绝，保留并发新数据 |
| review 5145721257 R5 | LOCAL 无线路/计数不完整 | Serializer/Repository；仍存在 | LOCAL 空线路合法且导出排除设备目录授权；counts 与实际 sections 相符 |
| review 5145721257 R6 | 编码 query/重复名字覆盖/错误泄露 | Guard/Repository；仍存在 | 逐 endpoint，解码参数名，合法 authority，固定类别错误；同名线路敏感值不能漏检 |
| review 5145721257 follow-up | input close 抛出后逃逸 | FileStore；仍存在 | try 包围完整 use 生命周期，CE 传播，provider 异常不进入 UI 文案 |
| issue 5560600608 | fbb2a74 自动状态摘要 | 历史信息 | 只作定位，不作当前通过证据 |
| issue 5590218370 | 8798b13 作者声明全部闭环 | 历史信息 | 实际该提交只改 filename fallback/测试，不包含 R1–R6 所需生产修复；按本表重新验证 |

## 新发现与限定补丁

除上述评论未修项，本轮失败复现还覆盖：字幕 textScale 极大值可进入正式 renderer；密码 CharArray 在拒绝重入/启动前取消时残留；重复 SAF callback 覆盖完成态；AtomicFile 发布失败可能仅记录系统日志；三处凭据/会话删除使用异步 apply；首页按 ID 缓存旧身份登录态；新恢复 ID 继承孤立凭据；逆向回滚可能把 after 身份凭据挂回 before 地址；旧网络登录响应晚到重新保存会话；生成的线路 ID 抢占另一来源的全局主键；同源新登录未推进认证代际导致旧响应可清新会话；首页 getServer 迟到的旧快照绕过结果代际。

身份相关变更仅是备份恢复必要接线：TokenStore 与 Emby/Jellyfin AuthProvider 的代际检查、删除落盘、两个 Factory 的独立 API/media client 首个 interceptor，以及 HomeViewModel 的身份变化展示失效和读取前后版本核验。没有扩展 Provider 能力或网络层重构。旧地址的既有请求不会被改写到新地址；失效 Provider 的后续新请求不出网且不得再次提交/清理鉴权结果，恢复后由 registry 新建 handle，普通同源重新登录仍可用。导入线路的 UUID 在预览时生成并冻结，续作不重新生成，已有本机线路 ID 保持原值。

CI 仅追加 `:core:model:test :provider:api:test :metadata:test` 到已有 Unit tests 步骤，不改变 required check 名、取消规则、重试策略或 `setup-gradle@v5` 的现行 ADR 决策。新 workflow 必须在 Agent A 接回的实际 PR head 上重新验证。

## 实际验证记录

验证使用 Windows Gradle wrapper 8.14、Android Studio JBR 21（源码 target 17）、SDK 36。未修改系统 JAVA_HOME。最终完整命令如下，单 worker 和进程内 Kotlin 编译只用于本机内存约束，不改变 CI 的测试或断言：

```powershell
$env:JAVA_HOME='C:\Program Files\Android\Android Studio\jbr'
$env:ANDROID_HOME='C:\Users\55160\AppData\Local\Android\Sdk'
.\gradlew.bat testDebugUnitTest :core:model:test :provider:api:test :metadata:test assembleDebug lintDebug :app:assembleDebugAndroidTest --rerun-tasks --continue --no-parallel --max-workers=1 '-Dorg.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8' '-Pkotlin.compiler.execution.strategy=in-process' --no-build-cache --no-daemon --console=plain
git diff --check
```

代码提交 `7e314c64cd209fdccb7ce211b8864bea767881e4` 的全量命令退出码 0，BUILD SUCCESSFUL，耗时 15m52s，1197 个 actionable task 均执行。assembleDebug 与 lintDebug 完成；22 份 lint XML 合计 0 error/fatal、19 条 warning 记录（含跨模块重复），不宣称零警告。后续提交若仅调整仪器测试/文档，生产 APK 与该提交的对应关系另以 SHA256 绑定。

最终单测 XML：**768 tests，0 failures，0 errors，0 skipped**。22 个有测试源码的 task 均 EXECUTED，无测试任务来自 UP-TO-DATE/FROM-CACHE；另外 `:player:mpv:testDebugUnitTest`、`:provider:webdav:testDebugUnitTest`、`:metadata:test` 为 **NO-SOURCE**，不计通过数。其他无内容的配置/资源任务仍可能 UP-TO-DATE 或 SKIPPED，不与测试用例混计。

| 模块 | 实际 tests | 模块 | 实际 tests |
| --- | ---: | --- | ---: |
| app | 8 | core/common | 67 |
| core/database | 9 | core/logging | 12 |
| core/model | 35 | core/network | 14 |
| core/security | 17 | core/ui | 4 |
| feature/detail | 37 | feature/home | 15 |
| feature/library | 44 | feature/player | 33 |
| feature/search | 27 | feature/server | 21 |
| feature/settings | 100 | player/compatibility | 9 |
| player/engine | 41 | provider/api | 8 |
| provider/base | 4 | provider/emby | 164 |
| provider/jellyfin | 88 | provider/local | 11 |

备份专项为 **140**（core/common 的 Crypto 8 + Serializer 22 + Validation 10，feature/settings 100）；这是明确选定的包级统计，不包括 Home/安全状态/两个 Provider 的必要集成回归。完整模块及类级数字、XML 时间戳和任务状态保存在本地 `final-unit-summary.json` 与 `final-unit-xml/`，均来自本轮实际运行。

下列均为上述实际 XML 中的类级结果，不是额外叠加的测试数；文件位于对应模块 `src/test/kotlin` 包下。

| 回归文件 | tests | 可直接定位的约束/反向断言 |
| --- | ---: | --- |
| BackupCryptoTest / BackupSerializerTest / BackupValidationSafetyTest | 8 / 22 / 10 | 导入认证、版本/数量/字段、LOCAL 自导入、敏感 query、输出与输入共同限制 |
| BackupRepositoryTest / BackupRestoreSafetyTest | 20 / 15 | `merge preserves local default when incoming new source is default`；`failed credential invalidation never overwrites source or progress`；`preview baseline change blocks restore without writes or credential clearing`；新来源孤立凭据、逆向回滚身份与线路主键冲突 |
| ProductionBackupDataSourceTest | 5 | `SQLite abort after first server write rolls back servers endpoints and progress`，真实 SQLite trigger，精确 before/after |
| SharedPrefsRestoreJournalTest | 19 | commit false、缓存不可信、实例互斥、损坏/不存在/物理 XML/.bak 区分、禁止覆盖未完成恢复 |
| RestoreSnapshotStoreTest | 7 | `private codec roundtrips every persistent field and nullable defaults`；发布读回失败；加密文件重开、损坏认证与精确 rollback；与 OS kill 证据分开 |
| ProductionBackupFileStoreTest | 12 | `oversize stream stops at the limit plus one byte without consuming remaining data`；取消/close、固定错误不泄露路径、实际包版本 |
| ProductionCredentialRemovalTest | 6 | Secret/Emby/Jellyfin 删除返回前同步提交，commit 失败必须报告，其他记录保持不变 |
| BackupViewModelTest | 16 | 事件消费/重建/重复 callback、拒绝重入和启动前取消擦密码、错误密码重试、冻结策略与两层确认 |

必要集成回归另见 TokenStoreAuthenticationTest、HomeViewModelTest、EmbyAuthProviderTest、JellyfinAuthProviderTest、EmbyFactoryRestoreIsolationTest 与 JellyfinFactoryRestoreIsolationTest。它们约束迟到提交/401/logout 不清除新登录，普通同源重新登录可用，撤销旧 handle 后 API/media 新请求数为零，未变来源与新 handle 正常请求，以及迟到 DB 读取和删除再添加不能复活旧首页身份。

仪器测试提交为 `337c626331dcd7552d13d85e76c0c9773883af4d`，只修改/新增 androidTest，没有再次修改生产源码。最终增量 `:app:assembleDebugAndroidTest :app:lintDebug` 退出码 0，BUILD SUCCESSFUL 2m2s，925 actionable task 中 9 executed、916 UP-TO-DATE；该命令没有重新跑单元测试，不能把缓存任务当成新的 768 项通过。

最终安装的生产 Debug APK SHA256 为 `F0217A87EE583E4BDB29351ACAFF2EF38EC68D0569A62D44C42E4DF4CC29CA33`，与全量验证的生产产物相同；androidTest APK 为 `0A68F7B22DD09FD1EB588606D1BE2AA79208470B47E49649421C35E771EDCFA4`。只操作本轮新建的 `Codex_PR18_Review_36` / `emulator-5556`，未操作物理设备或已有模拟器。

正式 UI 最终执行 `BackupUserFlowTest` 为 **1/1 通过，137.893 秒**（`final-device-3/ui-saf.log`）：真实 Settings 入口、SAF 取消且无文件、实际保存文件、自身导入规则和真实 appVersion、错误密码零写入、重试后预览零写入、实际点击未确认的禁用按钮仍无写入、勾选确认后 Room 数据及偏好恢复均有断言。两次创建器实际出现耗时 25.758s / 15.391s，OpenDocument 为 2.248s；这不是帧率、功耗或真机速度证明。

UI8 的失败是夹具误读 Compose 的 enabled=true 文字子节点；现场实际 clickable 祖先为 enabled=false。最终测试改为检查/点击真实 action，并保留反向数据断言。UI4–7 的 15 秒失败仍保留，最终测试对普通 UI 维持 15 秒、对实际密码计算使用单独 60 秒有限观察；没有生产超时修复、KDF 降级、自动重试或删断言。

最终恢复脚本退出码 **0**，共 **10 个验收调用通过**（UI 1 + 新进程恢复 8 + 损坏日志 1）。9 次 seed 调用按设计由 `Process.killProcess` 真正终止，产生预期 `Process crashed`，不算 9 项测试通过。均使用上述相同生产/测试 APK，日志在 `final-device-3/`，与早期旧 APK 试跑分开。

| 真实终止点 | 被终止 PID → 验证 PID | 实际断言 |
| --- | --- | --- |
| SNAPSHOT | 8522 → 8565 | 未登记日志，业务保持 before，孤立保护文件按探针清理 |
| PREPARING | 8607 → 8654 | 从保护 image 续作，变更/新增身份先失效 |
| INVALIDATED | 8696 → 8740 | 重复失效可续作，旧凭据不复活 |
| DB_BEFORE_MARK | 8781 → 8827 | Room 已写但标记未落盘，识别 after 后幂等继续 |
| DB_WRITTEN | 8868 → 8912 | 继续偏好阶段，核验最终来源集合/变更地址、37 条进度、未选进度与偏好 |
| PREFERENCES_APPLIED | 8952 → 8998 | 完成标记与清理，不重复破坏业务数据 |
| ROLLING_BACK | 9040 → 9084 | 精确还原 before，删除新增行，恢复 nullable 偏好及完整字段 |
| COMPLETED | 9124 → 9188 | 完成态只清理，不倒退为回滚 |
| 磁盘 XML 损坏 | 9228 → 9277 | NeedsAttention，新的恢复被拒，源/进度/偏好零写入，原源 Token 仍存在 |

8 个正常恢复用例都再次调用恢复并断言 NothingToRecover、数据不变。before/rollback 分支比较完整 fixture 快照；forward 分支核验来源集合、变更地址、进度数量、旧选中进度移除、未选进度与偏好，不声称逐条比较了全部 37 条最终进度记录的每个字段（导入 36 条，另保留本机未选 1 条）。完整字段 codec/回滚的其他非默认取值由独立 JVM/Robolectric 用例约束。损坏用例的 PID 来自持久 marker 与 TestRunner logcat（测试也断言不同进程），不是根据等待时间推断；该用例只断言原源 Token 非空，没有逐值比较全部凭据存储。

另行运行 `BackupCryptoTimingTest` **1/1 通过，11.027 秒**：独立进程 9358、默认 provider BC，生产 600,000 次参数。首次墙钟 5.839s / 线程 CPU 4.285s，第二次 5.110s / 3.750s；派生期间和之后主线程 pulse 均在 1 秒内返回。这个无活动页面的微测与 UI 中 15–26 秒的等待属于不同负载条件，不能推定所有差值的原因、帧率或真机性能，也不证明 JCA 同步函数可强制取消。

可机器核对的 SHA、命令参数、模块统计、APK 安装后哈希和每阶段日志哈希见 [validation JSON](pr18-agent-b-validation-2026-09-09.json)。复跑命令：

```powershell
.\scripts\verify-backup-restore.ps1 -Serial emulator-5556 -OutputDirectory D:\deepseek_test\pr18-agent-b-evidence\final-device-3 -IsolatedEmulator
& "$env:ANDROID_HOME\platform-tools\adb.exe" -s emulator-5556 shell am instrument -w -r -e backupAcceptance isolated -e class com.mediahub.app.backup.BackupCryptoTimingTest com.mediahub.app.test/androidx.test.runner.AndroidJUnitRunner
```

该序列号只对应本次专用模拟器；其他复审者必须选自己的专用环境。没有真实服务器协议或真机验收升级声明。

第一次全量命令在默认并发下出现 Windows 1455/JVM mmap/metaspace 分配失败，Gradle daemon 退出，**失败/中断，不算 PASS**。随后降低资源并发重新运行，没有跳过测试、增加自动重试或删除断言。两次 PowerShell 未完整引用 `-Pkotlin.compiler.execution.strategy` 的参数解析失败也保留为执行环境错误，不作测试结果。SAF 仪器 UI1–3 的选择器/shell 夹具错误以及 UI4–6 的 15 秒等待失败均保留。UI5/6 在本轮 Gradle 已停止后仍失败，故不归因于编译竞争。UI7 保留同一次导出，在 15 秒抓到后台线程 BC HMac/PKCS5S2 → BackupCrypto.deriveKey 的实际栈，主线程 pulse 成功；无重试情况下分别在 19.225s、19.972s 打开真实 SAF，证明此设备上 15 秒的加密等待假设不足。生产参数未降低；完整链的最终结果另列，不能用诊断续等将早期失败追认为通过。

预修复失败证据：UI/Repository 合计 29 项 13 失败；其中 FileStore 的“缺失输出目录”一项受 Robolectric 默认空写流影响，是测试 fixture 失败，不能作为生产泄漏复现。该用例后续改为真正抛错的输出流并保留错误脱敏及 write/close 次数断言。其余记录包括：验证器 8/8 失败；Journal 在未改生产 detached baseline 上 11 项 10 失败；Home 7 项 2 失败；持久删除/文件发布 7/7 失败；字幕极值 1/1 失败；新 ID 孤立凭据 1/1 失败；逆向回滚鉴权 1/1 失败；两个 provider 真实 MockWebServer barrier 登录响应迟到分别失败。编译失败尝试单独保留，绝不当成失败复现通过。

## 仍需外部复审的范围与边界

Agent A 最小复审覆盖四组：计划与数据（冻结身份/策略/UUID、事务内基线、完整 image 与 DataStore 反向恢复）；持久化与中断（发布读回、commit(false) 不可信缓存、物理 XML/.bak、续作/ROLLING_BACK/COMPLETED）；认证隔离（Singleton TokenStore 每 ID mutex/epoch/restore lease、成功登录推进 epoch、两 AuthProvider 的迟到登录/401/logout、Factory API/media guard、同步持久化 remove、同源保留）；正式 UI（Home 身份与读取代际、SAF 消费/取消/重入、密码重试和替换确认）。Agent C 独立重跑全套自动化并在可独占设备补 OEM SAF/后台重建证据。需将补丁接回 PR #18 后重新获取该 head/base 的 CI；当前远端绿色记录不能代替它。

恢复日志损坏、旧日志没有完整 image、设备密钥失效或中断后本机另有写入时会保留现场并拒绝继续覆盖；这是明确的待人工处理边界，页面没有一键丢弃日志的绕过功能。凭据失效不可逆，失败回滚可能需要重新登录。snapshot 发布后而 journal 登记前中断，以及完成后的 snapshot 删除失败可能留下加密 orphan 文件，尚无自动 GC；这是保留的磁盘清理风险。模拟器进程终止不证明掉电时全部存储介质的持久性。真机未执行时保持 **DEVICE UNVERIFIED**。

## 后续队列（本轮未实施）

依次为 2A Media3 renderer/track 映射、2B SLOW-FINAL COMPLETION、2C EndpointTestService 取消、PR #10 产品集成、Jellyfin 1G 协议/服务器验收、其余文档历史逐文件整理。没有新增视觉、WebDAV 云备份或其他 Provider 功能。

PR #16 原始六份日志可读取，ticks 换算核对正确（996830000 ticks = 99.683 秒）。新发现归档索引错误：`coldfinal2_wire.txt`（278 字节，SHA256 前缀 `9a4d1cd9fe0c5e2f`）实际是 18:59:38.474→18:59:42.262 的慢 Playing 204/3787 ms，无 Stopped，应对应慢失败样本；19:01 的成功来自 `coldfinal3_full.txt`，其中另有媒体 GET 502，不能证明播放位置持续推进。六份材料不含完整 UserData 原始响应或独立 BACK 时间戳。**SLOW-FINAL COMPLETION 保持 OPEN**，本轮未修改证据 PR 或推定取消根因。
